fastapi>=0.110
uvicorn[standard]>=0.27
python-dotenv>=1.0

# Phase 4 (BLE). Not imported yet — see BLE_NOTES.md 4.2 for why the transport
# is behind an interface with more than one possible backend.
# bleak>=0.22
